# Previous defense prototype

Archived before implementing the new root GDD. `source/` preserves the previous
game, including its tests; `e2e/` preserves browser scenarios for the old design.
These files are reference material, excluded from the active build. Their
`@/games/defense` imports refer to the original location; restore the directory
there to run the old implementation. Shared engine and platformer remain active.
